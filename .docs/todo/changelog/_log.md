# Execution Log

## 2026-05-08

- Forked IX-Zig into `E:\Workspaces\01_Projects\01_Github\ix-zig`.
- Implemented the first exact trigram runtime acceleration slice as a file-level negative gate over mandatory byte evidence.
- Kept case-insensitive requests verifier-only.
- Added `trigram_acceleration` telemetry to JSON stats.
- Validated unit tests, ReleaseFast build, and predecessor parity smoke.
- Probed and rejected a per-file trigram sketch because it preserved correctness but added median latency; kept exact StringZilla-backed trigram probes as the retained slice.
- Captured fork-local Rust-vs-Zig benchmark evidence in `.docs/reports/zig-vs-rust-fork-latest.json`; all 12 cases preserved match, byte, and file parity, with Zig winning 5/12.
- Compared the original Zig source tree against the standalone fork, reintroduced upstream whole-buffer stats-only fast count, and captured old-Zig/new-Zig/Rust validation in `.docs/reports/zig-upstream-port-validation-latest.json`.
- Probed original Zig atomic work claiming and rejected it for the current corpus after static partitioning won 9/12 with parity intact.
- Retained original Zig low-overhead discovery/read-path imports after `.docs/reports/zig-low-overhead-vs-retained-baseline-latest.json` showed 11/12 wins against the retained baseline with zero parity failures.
- Retained a `64 KiB` trigram activation floor after `.docs/reports/zig-trigram-size-gate-candidate-latest.json` showed 9/12 wins against ungated trigram with zero parity failures.
- Captured final promoted trajectory in `.docs/reports/zig-final-promoted-trajectory-latest.json`: final Zig won 9/12 versus Rust, split 6/6 versus original Zig, and preserved match/byte parity against both.
- Ran dupe-audit closeout on `src/core/search.zig`; no candidate duplicate pairs or exact duplicate candidates were reported.
- Converted the original-Zig split into a stronger result by retaining silent small-file trigram skip and a narrow dynamic literal scheduler; `.docs/reports/zig-hybrid-dynamic-literal-candidate-latest.json` shows 9/12 wins versus original Zig with zero parity failures.
- Rechecked the retained hybrid against the prior promoted binary in `.docs/reports/zig-hybrid-vs-prior-promoted-retention-latest.json`; current won 7/12, prior won 5/12, average current/prior ratio `0.9884`.
- Re-ran dupe-audit after the hybrid scheduler patch; no candidate duplicate pairs or exact duplicate candidates were reported.
- Built and compared the Zig candidate against the Windows-installed native `ix.exe` at `C:\Users\Savage\AppData\Local\Programs\iEx\bin\ix.exe` using strict sequential native-then-Zig runs. `.docs/reports/zig-vs-native-installed-ix-latest.json` shows Zig won 10/12, native won 2/12, zero parity failures, average Zig/native ratio `0.7205`.
- Rebuilt the updated parent fork at `E:\Workspaces\01_Projects\01_Github\iEx\zig` and compared it sequentially against the fork. `.docs/reports/zig-current-vs-updated-parent-latest.json` showed current won 11/12, parent won 1/12, zero parity failures.
- Imported the only parent-winning mechanism as a narrow scheduler extension: dynamic work claiming now also covers single-predicate `regex_literal_alternates` scans with `<=128` files. `.docs/reports/zig-parent-alternation-import-candidate-latest.json` showed the candidate won 12/12 versus the updated parent and 7/12 versus the pre-import baseline, with zero parity failures.
- Re-ran the parent alternation scheduler import as an explicit before/after gate in `.docs/reports/zig-before-after-parent-alternation-gate-latest.json`: after won 8/12, before won 4/12, zero parity failures, average after/before ratio `0.9933`. The alternation lane itself was noisy in this run, but suite-level retention stayed positive, so the bounded route remains retained.
- Ran the focused `regex_literal_alternates` morphology proof against `.docs/reports/bin/ix-zig-current-before-parent-import.exe`: `.docs/reports/zig-alternation-morphology-proof-latest.json` shows after won 4/5 generated 100-file alternation variants over 60 sequential samples per case, zero parity failures, average after/before ratio `0.9855`; `.docs/reports/zig-alternation-adjacent-guard-latest.json` shows zero adjacent regressions above 2%, so the bounded route is promoted in `.docs/reports/bin/ix-zig-promoted-alternation-morphology.exe`.
- Ran dupe-audit on `src/core/search.zig` after promotion; `segment_count=29`, `candidate_pair_count=0`, and `exact_duplicate_candidate_count=0`.
- Started the compiled corpus machine lane with `src/core/corpus.zig`, a private proof-program lowering layer over `ExpressionPlan` and `trigram.Admission`; `ix explain` now emits `proof_program` with query class, mandatory evidence, posting placeholders, verifier class, and fail-closed fallback. Documented the mmap/bitset/index architecture in `.docs/research/2026-05-08-compiled-corpus-machine.md`.
- Ran dupe-audit on `src/core/corpus.zig` and `src/cli/output.zig`; `segment_count=12`, `candidate_pair_count=0`, and `exact_duplicate_candidate_count=0`.
- Ran the compiled-corpus proof-program predecessor gate against `.docs/reports/bin/ix-zig-promoted-alternation-morphology.exe`. `.docs/reports/zig-compiled-corpus-proof-predecessor-gate-latest.json` showed current won 9/12, predecessor won 3/12, zero parity failures, average current/predecessor ratio `0.9830`; the only >2% regression candidate (`large-dir-literal`) was falsified by `.docs/reports/zig-compiled-corpus-proof-large-literal-rerun-latest.json` over 101 samples with ratio `0.9796`, so `.docs/reports/zig-compiled-corpus-proof-retention-latest.json` keeps the slice and promotes `.docs/reports/bin/ix-zig-promoted-compiled-corpus-proof.exe`.
- Compared both Zig promoted binaries against installed native IX at `C:\Users\Savage\AppData\Local\Programs\iEx\bin\ix.exe`. `.docs/reports/zig-predecessor-vs-installed-native-latest.json` showed predecessor won 11/12 with zero parity failures and average predecessor/native ratio `0.7142`; `.docs/reports/zig-current-vs-installed-native-latest.json` showed current won 12/12 with zero parity failures and average current/native ratio `0.6983`.
- Scrubbed `README.md` for public GitHub readiness: removed workstation-local paths and user identifiers, replaced machine-specific build/harness instructions with environment-variable contracts, and normalized internal performance wording into public verifier-boundary language.
- Rewrote sentence-level `README.md` prose for enterprise open-source positioning: preserved command/build tables and technical structure while replacing internal benchmark-race phrasing, overclaim language, and speculative acceleration copy with correctness-first production language.
- Rebuilt `README.md` into a concise public project README: kept the technical signal around IX command parity, AVX2/StringZilla, strategy-aware regex dispatch, exact trigram pruning, corpus epochs, and benchmark gates while removing notebook-style runtime exposition and long speculative deep dives.
- Integrated PCRE2 10.44 with JIT compilation as the primary regex engine, replacing the Zig-native recursive backtracking engine on the `regex_full` and `else` strategy paths. Vendored PCRE2 source at `.refs/pcre2/` with static `config.h` enabling `SUPPORT_JIT`, `SUPPORT_UNICODE`, and `PCRE2_STATIC`. Created `src/core/pcre_regex.zig` with threadlocal single-entry compile cache wrapping `pcre2_compile_8` / `pcre2_jit_compile_8` / `pcre2_match_8`. Patched 3 call sites in `src/core/search.zig` (`countRegexStatsOnly` column, `countRegexStatsOnly` count, `regexWithLiteralPrefilter` column) using error-union fallback: `pcre_regex.X(...) catch regex.X(...)`. Added 27 PCRE2 C translation units to `build.zig` via `addCSourceFiles`. Benchmark results: `en-no-literal` from ~30s to 229ms (matches ripgrep at 239ms); `linux-no-literal` from 9,922ms to 2,930ms (3.4x speedup); `en-surrounding-words` from ~4,500ms to 384ms (~12x speedup). Updated `README.md` with PCRE2 badge, What Is Inside table rows, strategy routing entries, source layout, vendoring note, and roadmap updates (DFA/VM research items superseded). Created `.docs/changelog/130-pcre2-jit-regex-integration.md`.
- Ran the 2026-05-11 edge-selection pass with Insect search/transcript harvesting and a fresh Rust IX comparison on the Linux bench corpus. `tools/reports/latest.json` shows Zig `681.5082ms` versus Rust `620.3549ms` on `re:\bPM_RESUME\b`, with `174.7226ms` discovery and `506.5152ms` scan across `79088` discovered files. Captured the selected next slice in `.docs/research/2026-05-11-zig-search-edge-next-step.md`: promote the existing proof-program concept into executable `PathAdmission -> FileAdmissionBytecode -> ByteKernel -> LineVerifier`, with one-pass trigram admission, PCRE2 metadata lowering, and narrowly-scoped C byte kernels.
- Promoted the admission-bytecode lane into repo-facing documentation: updated `README.md` headline, architecture flow, execution model, What Is Inside table, trigram section, new Admission Bytecode Lane section, source-facing terminology, and Roadmap priorities. Added `.docs/changelog/131-admission-bytecode-pipeline-documentation.md` so the planned `FileAdmissionProgram`, opcode set, PCRE2 metadata-lowering contract, and C byte-kernel boundary are traceable outside the research artifact.
- Implemented the first executable admission-bytecode slice as `TrigramAdmissionProgram` in `src/core/search.zig`: compile-once trigram membership, group masks, duplicate-slot suppression, per-group satisfaction counters, and one-pass rolling 24-bit trigram admission threaded through serial, mmap, and worker scan paths. Added a contract-safe whole-file mandatory-needle admission for single-predicate literal / prefix / suffix / regex-word-boundary plans before trigram execution; on `re:\bPM_RESUME\b` this reduced direct-profile trigram candidate buffers from `2578` to `1`. Added AND/OR admission contract tests, updated `README.md`, and documented the slice in `.docs/changelog/132-one-pass-trigram-admission-program.md`. Validation: `zig build test`; Linux bench corpus `re:\bPM_RESUME\b` 9-sample gate now reports Zig `652.9729ms` vs Rust IX `631.6790ms`, reducing the same-turn gap from `9.86%` to `3.37%`.
- Ran dupe-audit closeout on `src/core/search.zig` for the admission-program slice: `segment_count=42`, `candidate_pair_count=0`, and `exact_duplicate_candidate_count=0`.
- Added the first warm-start trigram candidate cache for stats-only eligible searches. Cold scans now record shard-local candidate/pruned/skipped facts and write `.ix-trigram-warm-{key}.cache`; warm scans validate expression/root/path-set and scan only the cached candidate frontier. Direct Linux corpus `re:\bPM_RESUME\b` measurement: cold fill `707.0576ms`, warm reuse `167.6123ms` total with `7.2934ms` scan, `79041` cached-pruned files, `44` candidate files; Rust IX direct baseline `567.2822ms`. Boundary documented in `README.md`, `.docs/research/2026-05-11-zig-search-edge-next-step.md`, and `.docs/changelog/133-warm-trigram-candidate-cache.md`: current cache is path-set validated for stable corpora, not a content-fresh persistent index.
- Corrected the warm-start foreground drift into the hidden Nexus evidence-frontier sidecar. Public `search` / `matches` now finish the normal foreground report, spawn silent `__ix_nexus` in the background, and never synchronously write the cache artifact; only the sidecar writes `.ix-evidence-{key}.cache`, while later public searches consume a validated frontier if present. Current 5-sample Linux corpus gate: foreground cold + sidecar spawn median `741.6188ms` total / `541.8612ms` scan with evidence-pruned `0`; evidence-hot median `186.6524ms` total / `8.4236ms` scan with `79041` evidence-pruned files; prior promoted Zig median `847.5781ms`; Rust IX direct median `600.9322ms`. Updated `README.md`, `.docs/research/2026-05-11-zig-search-edge-next-step.md`, and `.docs/changelog/133-warm-trigram-candidate-cache.md`.
- Tightened the Nexus sidecar launch after wall-clock validation exposed foreground process time was still paying sidecar lifetime cost through `std.process.spawn`. Replaced the Windows sidecar launcher with direct `CreateProcessW` using `DETACHED_PROCESS`, `CREATE_NEW_PROCESS_GROUP`, `CREATE_NO_WINDOW`, explicit command-line quoting, and immediate process/thread handle close. Updated 5-sample gate: current foreground cold + sidecar median `626.9509ms` wall / `616.5599ms` engine total / `459.9087ms` scan; current evidence-hot median `189.9459ms` wall / `181.4790ms` engine total / `7.6543ms` scan with `79041` evidence-pruned files; prior promoted Zig median `734.5797ms` wall / `727.5432ms` total; Rust IX median `643.2430ms` wall / `625.0220ms` total. Report saved at `.docs/reports/nexus-evidence-regression-check-2026-05-11.json`; build/test gates passed after the correction.
- Locked the hidden Nexus evidence frontier checkpoint in commit `51f8b3b` and ran a before/after Linux corpus gate for the next content-epoch slice. Added `IXEVIDENCE2` cache identity with write-side `content_signature=hash(path,size,inode/file-index,mtime)`, rejected foreground full-tree metadata validation after it made hot reuse multi-second on Windows, and gated redundant sidecar launch after successful evidence-pruned foreground reuse. Final 9-sample report `.docs/reports/content-epoch-postchange-2026-05-11.json`: evidence-hot average wall `165.0266ms` / median wall `164.1002ms` / median total `157.1446ms` / median scan `7.0478ms`, improving the committed baseline hot median wall `203.6417ms` and scan `8.1764ms` while preserving `79041` evidence-pruned files. Updated `README.md`, `.docs/research/2026-05-11-zig-search-edge-next-step.md`, `.docs/changelog/133-warm-trigram-candidate-cache.md`, and added `.docs/changelog/134-nexus-content-epoch-identity.md`.
- Added adversarial Nexus evidence-frontier test coverage across `src/cli/args.zig`, `src/core/search.zig`, and `src/main.zig`: hidden `__ix_nexus` parse forcing, `IXEVIDENCE2` cache round-trip, stale/malformed header rejection, candidate-count overflow rejection, active-file narrowing with cached pruned/skipped accounting, content-signature mutation detection, end-to-end `search.run` evidence-frontier consumption, sidecar launch gating after evidence-pruned reuse, and Windows command-argument quoting. Validation: `zig build test`; `zig build -Doptimize=ReleaseFast`; dupe-audit summary `target_file_count=3`, `segment_count=66`, `candidate_pair_count=0`, `exact_duplicate_candidate_count=0`. Added `.docs/changelog/135-nexus-adversarial-test-coverage.md`.
- Repaired Windows protected-tree behavior so `AccessDenied` is a structured partial-search diagnostic instead of a fatal discovery abort. Added `SearchStats.access_errors` with bounded samples, phase/operation counters, and `status:"partial"` output for completed searches with denied filesystem edges. Discovery now records denied directory-open / iteration events and continues; serial and shard scan paths record denied file-open/read events without suppressing them as invisible skips. Validation: `zig build test`; `zig build -Doptimize=ReleaseFast`; `C:\Windows\System32` rare no-hit now exits with parseable JSON, `status=partial`, `files_discovered=19636`, `access_denied=59`, `total_ms=4697.133`; full `C:\Windows` rare no-hit completed with `files_discovered=171462`, `access_denied=107`, `total_ms=15522.5315`; dupe-audit over `src/core/search.zig`, `src/core/stats.zig`, and `src/cli/output.zig` reported `segment_count=67`, `candidate_pair_count=0`, `exact_duplicate_candidate_count=0`.
- Sealed the evidence-frontier P1 freshness gap flagged in review: foreground cache reuse now recomputes the live content signature and rejects stale `.ix-evidence-*.cache` artifacts before narrowing the active file set. Added a regression test that writes an evidence cache, mutates the underlying file, and proves `loadEvidenceFrontierCache` fails closed. Updated `README.md` to remove the stale manual-delete warning and state the current portable metadata epoch boundary. Validation: `zig build test`; `zig build -Doptimize=ReleaseFast`; mutation smoke over `.docs/tmp/freshness-smoke` returned `before_matches=1`, `after_matches=2`, `after_pruned=0`; dupe-audit reported `segment_count=68`, `candidate_pair_count=0`, `exact_duplicate_candidate_count=0`.
- Corrected the evidence-frontier activation regression exposed by the Rust-vs-Zig best-of-three report: the cache path-set/content signatures were order-dependent, so sidecar discovery order drift could reject a valid cache before foreground narrowing. Replaced both signatures with order-insensitive `xor + wrapping-sum + count` reductions over per-file Wyhash identities and added a regression test proving reversed discovery order preserves both signatures. Fresh current-binary Linux corpus proof after rebuilding `.ix-evidence-e98d5d68a37c0b3.cache`: foreground now consumes the cache (`pruned=79041`, `matches=9`) but still spends `2669.8766-2982.7649ms` total because portable foreground content freshness stats the full file set. This is a correctness-safe improvement over the broken `pruned=0` path, but still loses to Rust; next required slice is NTFS USN/change-journal freshness, not another full metadata walk. Validation: `zig build test`; `zig build -Doptimize=ReleaseFast`; `git diff --check`; dupe-audit reported `segment_count=69`, `candidate_pair_count=0`, `exact_duplicate_candidate_count=0`.
- Added an explicit no-Nexus control plane for cold comparator runs: public `search` / `matches` set `request.nexus_disabled` from `IX_NEXUS=0|false|off`, `prepareEvidenceFrontier` returns before cache-load/signature work, and the sidecar launch gate is disabled at the same request boundary. Stable 5-sample Rust-first no-warm benchmark saved under `.docs/reports/bench-rust-vs-zig-no-nexus-2026-05-11/`: `ix-zig-src` median Zig `8.957ms` vs Rust `9.873ms` with parity `27/27`; Linux `re:\bPM_RESUME\b` median Rust `1022.323ms` vs Zig `1023.278ms` with parity `9/9` and Zig evidence-pruned `0`; `en.sample.txt` `lit:Sherlock Holmes` median Rust `36.373ms` vs Zig `104.806ms` with parity `30/30`. Verdict: without Nexus/warm evidence, Rust wins the real dataset score `2/3`; previous warm-cache lead claims must be treated as cache-hot only, not cold-engine superiority. Validation: `zig build test`; `zig build -Doptimize=ReleaseFast`; `git diff --check`.
- Ran a one-round cold-engine route-selection pass and retained the smallest positive code slice: stats-only literal counting now calls the existing StringZilla `sz.indexOf` wrapper instead of the pure-Zig `simd.indexOf` loop. Rejected the first candidate, a streamed multi-chunk literal counter, because it left Sherlock at ~`104ms` and added Linux noise. Retained StringZilla count because the 5-sample Rust-first no-Nexus gate `.docs/reports/bench-stringzilla-count-candidate-2026-05-11.md` moved the score to Zig `2/3`: `ix-zig-src` median Zig `9.072ms` vs Rust `9.788ms`, Linux `re:\bPM_RESUME\b` median Zig `1012.047ms` vs Rust `1067.677ms`, Sherlock still Rust `36.214ms` vs Zig `95.438ms`, all with match parity. Next quickest high-yield route is intra-file byte-range sharding for single large stats-only literal buffers; the Sherlock loss is one 144MB file, so outer file parallelism is idle. Validation: `zig build test`; `zig build -Doptimize=ReleaseFast`; `git diff --check`.
- Added the cold-engine intra-file byte-range shard fast path for large stats-only case-sensitive literal-compatible buffers. Non-single-chunk files now route through mmap-backed `scanFileMmap`; eligible buffers split into request-thread logical byte ranges with `needle.len - 1` overlap and seam-owned non-overlap counting. JSON now reports real `byte_shard_kernel` activation instead of the previous hardcoded inactive object. Rust-first no-Nexus 9-sample gate `.docs/reports/bench-byte-shard-fastpath-2026-05-11/summary.json`: `ix-zig-src-trigram` median Zig `9.433ms` vs Rust `10.151ms`; Linux `re:\bPM_RESUME\b` median Zig `1105.717ms` vs Rust `1112.179ms`; `en.sample.txt` `lit:Sherlock Holmes` median Zig `35.555ms` vs Rust `39.632ms` with byte sharding active over `8` ranges. All three retained match parity. Validation: `zig build test`; `zig build -Doptimize=ReleaseFast`; `git diff --check`; dupe-audit over `src/core/search.zig` and `src/cli/output.zig` reported `segment_count=70`, `candidate_pair_count=0`, `exact_duplicate_candidate_count=0`.
- Ran the next cold/hot route-selection pass after the Rust-win correction. Rejected three slices: USN evidence-frontier epoching because this host did not expose a usable queryable journal epoch, StringZilla whole-file admission probes because the median moved the wrong direction, and 8-thread shuffle gating because Linux `re:\bPM_RESUME\b` regressed to ~`3.19s` median wall. Retained only the stats-only whole-buffer word-boundary literal counter for `regex_word_boundary_literal` / chunk-casefolded ASCII word-boundary plans, preserving line-count semantics and adding a falsifying test for repeated same-line matches plus embedded-token rejection. Final Rust-first no-Nexus 5-sample gate `.docs/reports/bench-retained-word-boundary-final-2026-05-11/summary.json`: Rust median wall `985.877ms`, Zig median wall `1141.258ms`, Zig/Rust `1.158`; median engine total Rust `968.1402ms`, Zig `1131.5291ms`, parity `9/9`. Verdict: retained slice is correct but not enough; Rust still wins this lane. Validation: `zig build test`; `zig build -Doptimize=ReleaseFast`; `git diff --check`.
- Re-aligned the Nexus architecture with the chat-log invariant that warm acceleration must behave like a background buffer, not a foreground tax. Added a live-clean evidence frontier fast path before discovery: public search consumes `.ix-evidence-{key}.cache` only when a sibling `.live` marker exists, then scans the retained candidate list directly with `discover_ms=0`; absent or malformed live markers fall back to ordinary cold search. Hidden `__ix_nexus` now owns that live marker after building the cache and blocks on `ReadDirectoryChangesW` for the searched tree, deleting the marker when the watched tree mutates or the sidecar exits. Public-path benchmark `.docs/reports/bench-live-frontier-public-sidecar-2026-05-11/summary.json`: cold foreground launch `1604.038ms` wall while spawning the detached sidecar; live replay median Zig `8.918ms` wall / `2.3435ms` scan / `0ms` discovery versus Rust median `1013.461ms`, a `113.642x` median speedup with match parity `9/9` and `79041` evidence-pruned files. Follow-up PID-owned live-marker confirmation `.docs/reports/bench-live-frontier-pid-guard-2026-05-11/summary.json`: cold foreground launch `1717.854ms`; live replay median Zig `10.145ms` wall / `2.5171ms` scan / `0ms` discovery versus Rust median `1036.194ms`, a `102.138x` median speedup. Validation: `zig build test`; `zig build -Doptimize=ReleaseFast`; `git diff --check`; dupe-audit over `src/core/search.zig` and `src/main.zig` reported `segment_count=68`, `candidate_pair_count=1`, `exact_duplicate_candidate_count=0` with the single candidate limited to adjacent cache parsing chunks.
- Retained the cold-lane top-level parallel discovery slice after rejecting per-file streaming discovery. The rejected `std.Io.Queue(DiscoveredFile)` stream preserved Linux `re:\bPM_RESUME\b` match parity but regressed median wall to `2128.595ms` at `--threads 32`, so it was removed. The retained slice parallelizes only first-level directory subtrees into per-worker `FileList` shards, preserving the existing materialized scan contract. Linux no-Nexus `re:\bPM_RESUME\b` moved from current-before median `740.210ms` / discovery `~180ms` to median `585.413ms` / discovery `88.6457ms` at `--threads 32`, beating same-turn Rust median `720.832ms` with parity `9/9`. Guard lanes: `ix-zig/src lit:TrigramAdmissionProgram --threads 8` median `9.013ms`, `en.sample.txt lit:Sherlock Holmes --threads 8` median `33.540ms`. Artifacts: `.docs/reports/bench-streaming-cold-after-2026-05-11/`, `.docs/reports/bench-cold-current-post-stream-revert-2026-05-11/`, `.docs/reports/bench-rust-current-compare-2026-05-11/`, `.docs/reports/bench-parallel-discovery-after-2026-05-11/`, `.docs/reports/bench-parallel-discovery-guard-2026-05-11/`. Validation: `zig build test`; `zig build -Doptimize=ReleaseFast`; `git diff --check`; dupe-audit `segment_count=64`, `candidate_pair_count=0`, `exact_duplicate_candidate_count=0`.
- Documented the retained word-boundary byte-shard verifier-erasure slice in `README.md`, `.docs/research/2026-05-11-zig-search-edge-next-step.md`, and `.docs/changelog/136-word-boundary-byte-shard-verifier-erasure.md`. The docs now state the exact lowering contract (`re:\bLITERAL\b` -> `regex_word_boundary_literal` -> `ByteShardPlan.word_boundary_literal`), newline-owned range semantics, JSON telemetry fields, fallback boundary, and benchmark gate: Linux no-Nexus `re:\bPM_RESUME\b` median wall `609.966ms` versus previous reference `637.615ms` (`-4.34%`), parity `9/9`, `execution_mode=byte_sharded`, `byte_shard_kernel.strategy=word_boundary_literal`, `files_profiled=10`, `range_calls=40`, `line_aligned_ranges=40`; guard lane `lit:Sherlock Holmes` median `34.788ms` versus prior `38.043ms`; `lit:the` Linux median `613.823ms` with `1469971` matches. Boundary recorded honestly: retained improvement, not a 10x class jump; remaining PM_RESUME cost is discovery/open/admission and nested shard scheduling.
- Ran an Insect-backed deep research pass for the warm-layer question and saved the architecture note to `.docs/research/2026-05-11-warm-layer-sidecar-architecture.md`. Harvested GitHub Blackbird, Zoekt/GitLab, Lucene NRT, Watchman, Microsoft USN, Everything, FastFileWatch, Reddit, and YouTube transcript artifacts under `.docs/research/insect/warm-layer-2026-05-11/`. Conclusion: the current Nexus evidence frontier is a query-keyed invalidation cache, not a self-maintaining warm index; the next canonical path is `FileCatalog -> corpus-global trigram postings -> hidden __ix_indexd -> generation refresh -> USN delta apply`, with one binary, one index format, and fail-closed epoch publication.
- Committed the warm evidence frontier checkpoint as `aed5840`, created the `develop` branch, and decomposed the warm-index research into six planning-spec parent chains (`137` through `142`) with 90 lettered execution units under `.docs/todo/pending/`. Executed and archived `137a` through `137e`: baseline lock, catalog type skeleton, root identity model, deterministic path table builder, and file metadata/text-kind capture. Added inert `src/core/catalog.zig`, imported it into the test aggregation path, and validated with `C:\Users\Savage\.local\zig\zig-x86_64-windows-0.16.0\zig.exe build test`, `zig.exe build -Doptimize=ReleaseFast`, and `git diff --check`.
- Completed planning-spec chain `137` (`137a` through `137o`) for the warm FileCatalog foundation. Added versioned little-endian catalog serialization, wrong-root/truncation/bounds/sorted-shape validation, atomic publish, deterministic build bytes, inactive `stats.catalog_index` JSON telemetry, README boundary text, cold-search parity evidence, and research handoff status. Validation: `zig build test`; `zig build -Doptimize=ReleaseFast` after clearing a stale Windows output-binary process lock; `git diff --check`; dupe-audit over `src/core/catalog.zig`, `src/core/stats.zig`, and `src/cli/output.zig` reported `segment_count=26`, `candidate_pair_count=0`, `exact_duplicate_candidate_count=0`.
- Completed planning-spec chain `138` (`138a` through `138o`) for warm trigram postings. Added `src/core/postings.zig` with versioned root/generation-pinned trigram postings, binary-safe trigram extraction, sorted unique postings build, little-endian serialization/parsing, malformed/wrong-root/wrong-generation rejection, expression evidence lowering, AND/OR candidate evaluation, explicit `RequiresFullScan` fallback discipline, verifier handoff via catalog entry selection, inactive `stats.postings_index` JSON telemetry, README/research boundary docs, and adversarial tests for duplicate file pairs, no-evidence queries, unsafe disjunctions, malformed density, sortedness, and duplicate FileIds. Validation: `zig build test`; `zig build -Doptimize=ReleaseFast`; `git diff --check`; built-binary `search --json`, `matches --json`, and `explain` smokes; dupe-audit over `src/core/postings.zig`, `src/core/stats.zig`, and `src/cli/output.zig` reported `segment_count=32`, `candidate_pair_count=0`, `exact_duplicate_candidate_count=0`.
- Completed planning-spec chain `139` (`139a` through `139o`) for the hidden warm `__ix_indexd` lifecycle. Added hidden CLI parsing, main dispatch, repo-local `.ix/index` config, nonblocking root lock, process-owned heartbeat, foreground bootstrap marker, silent detached launcher reuse, explicit `IX_INDEX=1|true|on` public-search spawn gate, lifecycle/parity tests, README/research operator boundary docs, and binary parity proof that `IX_INDEX=0` and `IX_INDEX=1` produced identical public `matches` stdout with zero stderr while the enabled path created internal index state. Validation: `zig build test`; `zig build -Doptimize=ReleaseFast`; `git diff --check`; dupe-audit over `src/core/indexd.zig`, `src/main.zig`, and `src/cli/args.zig` reported `segment_count=21`, `candidate_pair_count=0`, `exact_duplicate_candidate_count=0`.
- Completed planning-spec chain `140` (`140a` through `140o`) for warm index generation refresh. Added `src/core/generation.zig` with `IXGEN001` manifests, generation directories, segment payload publication, atomic per-epoch manifests, stable `.ix/index/current.ixgen` refresh, fail-closed manifest validation, `ReaderPin` epoch retention, partial-publish rejection, search adoption guard helpers, inactive `stats.generation_refresh` JSON telemetry, README/research docs for `refresh_epoch` vs future `compact_epoch`, and a benchmark artifact showing current-manifest read proxy cost at `34.559us` against release search samples `10.787, 7.533, 6.615, 6.327, 6.705ms`. Validation: `zig build test`; `zig build -Doptimize=ReleaseFast`; `git diff --check` with LF-to-CRLF warnings only; dupe-audit over `src/core/generation.zig`, `src/core/stats.zig`, `src/cli/output.zig`, and `src/main.zig` reported `segment_count=39`, `candidate_pair_count=0`, `exact_duplicate_candidate_count=0`.
- Completed planning-spec chain `141` (`141a` through `141o`) for Windows-first USN delta apply. Added USN record identity extraction, catalog-reference resolution, delta task mapping, duplicate/rename-storm coalescing, continuity failure escalation, `ReadDirectoryChangesW` invalidation fallback, root-reconcile generation publication through canonical catalog/postings/generation owners, a Windows smoke script, and `.docs/architecture/warm-index-windows-freshness.md`. Validation: `zig build test`; `zig build -Doptimize=ReleaseFast`; `git diff --check` with LF-to-CRLF warnings only; smoke script `scripts\smoke-usn-journal-paths.ps1` passed 148/148 tests; dupe-audit over `src/core/usn.zig` reported `segment_count=23`, `candidate_pair_count=0`, `exact_duplicate_candidate_count=0`.
- Completed planning-spec chain `142` (`142a` through `142o`) for warm-index compaction ops hardening. Added compaction candidate planning, compacted generation publication, catalog tombstone folding, reader-safe generation GC planning, minimal index diagnostics, hidden `__ix_indexd --repair` reconcile marker, adversarial branch-switch/rename/delete-recreate tests, and typed cold/index-hot/mutation-hot/fallback performance gates. Updated `README.md` and `.docs/research/2026-05-11-warm-layer-sidecar-architecture.md` with the honest boundary: internal warm-layer substrate is implemented, but foreground acceleration from resident postings remains disabled until manifest adoption proves verifier-equivalent pruning. Final closeout verified all six warm-index chains (`137` through `142`) as 90 archived execution slices plus six archived parent files. Validation: `zig build test` at `163/163`; `zig build -Doptimize=ReleaseFast`; `git diff --check` exit 0 with LF-to-CRLF warnings only; dupe-audit over `src/core/catalog.zig`, `src/core/generation.zig`, `src/core/usn.zig`, `src/core/indexd.zig`, and `src/core/stats.zig` reported `segment_count=69`, `candidate_pair_count=1`, `exact_duplicate_candidate_count=0`, with the single non-exact candidate limited to catalog/generation header-format regions.
- Ran a three-way Windows stress comparison across installed old IX (`C:\Users\Savage\AppData\Local\Programs\iEx\bin\ix.exe`), Rust IX (`E:\Workspaces\01_Projects\01_Github\iEx\target\release\iex-cli.exe`), and current Zig IX. Added `.docs/reports/bench-old-rust-zig-current-2026-05-12/run-benchmark.ps1` after discovering and fixing a stdout-pipe benchmark deadlock. The run exposed a real current-Zig protected-tree contract bug: serial scans treated `FileBusy` as fatal while old/Rust returned usable partial JSON. Fixed `src/core/search.zig` to route recoverable scan-open/read failures through bounded `access_errors`, and extended the stats regression for `FileBusy`. Validation: `zig build test` passed `163/163` before and after the fix; direct `C:\Windows\System32` rare no-hit now exits `0` with `status:"partial"`. The long benchmark was stopped after host suspend contaminated one timing row; partial aggregate `.docs/reports/bench-old-rust-zig-current-2026-05-12/partial-summary.json` contains `722` raw runs, `718` valid non-smoke runs, one invalid suspend row, and winner counts old IX `83`, Rust IX `36`, current Zig `0`, incomplete `1`. Operational conclusion: the warm/background layer is silent, but this build is not net-faster on protected Windows corpora; foreground adoption of resident postings remains the next required acceleration boundary.
- Promoted the first correctness-safe foreground warm-index adoption slice. Public `IX_INDEX=1` searches now pass the opt-in into `SearchRequest`, hidden `__ix_indexd` publishes real catalog + postings payloads into `.ix/index/generations/<epoch>/`, writes a live-owner marker only for watch mode, and holds that marker until a non-`.ix` root mutation arrives. Foreground search pins `current.ixgen`, requires the live marker, lowers eligible expressions to postings lookup, maps candidate FileIds back through the catalog, and invokes the existing canonical scanner only on the retained files. Unsupported hidden/case-insensitive/follow-symlink/multi-root paths fail closed to normal search.
- Added epoch-pinned query-frontier reuse under `.ix/index/query/` so repeated identical warm searches avoid reparsing the full postings segment. Regression coverage now proves the two-stage path: live postings prune the candidate frontier, then a second identical search reports `generation_refresh.refresh_status="live_query_cache"` and scans only the cached candidates. Fixed a postings-format defect found by root-scale indexing by dropping the reserved zero trigram sentinel during extraction. Validation: `zig build test` passed `166/166`; `zig build -Doptimize=ReleaseFast` passed; dupe-audit over `src/core/search.zig`, `src/core/indexd.zig`, `src/core/postings.zig`, `src/main.zig`, and `src/cli/args.zig` reported `segment_count=118`, `candidate_pair_count=0`, `exact_duplicate_candidate_count=0`. Engine evidence on `src`: `lit:GenerationManifestHeader` old `9.6923ms`, Rust `3.9932ms`, Zig warm query cache `0.894ms`; `lit:ReadDirectoryChangesW` old `9.8619ms`, Rust `4.4165ms`, Zig `1.0456ms`; absent literal old `6.803ms`, Rust `4.6571ms`, Zig `0.4535ms`. Wall-clock on tiny corpora still shows Rust process-start advantage, so the regained lead is verified at the search-engine phase boundary, not yet at end-to-end process latency.
